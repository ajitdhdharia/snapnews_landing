"use client";
import { HeroParallaxPage } from "@/components/HeroParallaxPage";

export default function Page() {
  return (
    <main className="flex bg-white min-h-screen flex-col items-center justify-between p-24 bg-grid-small-black/[0.1]">
      <HeroParallaxPage />
    </main>
  );
}
